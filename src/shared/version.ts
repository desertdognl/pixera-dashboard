export const APP_VERSION = '1.0.7'

/** Last version whose live JSON/TCP path was tested against a real Pixera. Work after this is without a Pixera on the network. */
export const LAST_PIXERA_TESTED_VERSION = '0.6.0'
